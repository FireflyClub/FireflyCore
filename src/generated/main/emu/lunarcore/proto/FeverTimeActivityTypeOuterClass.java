// Code generated by protocol buffer compiler. Do not edit!
package emu.lunarcore.proto;

import us.hebi.quickbuf.ProtoEnum;
import us.hebi.quickbuf.ProtoUtil;

public final class FeverTimeActivityTypeOuterClass {
  /**
   * Protobuf enum {@code FeverTimeActivityType}
   */
  public enum FeverTimeActivityType implements ProtoEnum<FeverTimeActivityType> {
    /**
     * <code>FEVER_TIME_BATTLE_RANK_C = 0;</code>
     */
    FEVER_TIME_BATTLE_RANK_C("FEVER_TIME_BATTLE_RANK_C", 0),

    /**
     * <code>FEVER_TIME_BATTLE_RANK_B = 1;</code>
     */
    FEVER_TIME_BATTLE_RANK_B("FEVER_TIME_BATTLE_RANK_B", 1),

    /**
     * <code>FEVER_TIME_BATTLE_RANK_A = 2;</code>
     */
    FEVER_TIME_BATTLE_RANK_A("FEVER_TIME_BATTLE_RANK_A", 2),

    /**
     * <code>FEVER_TIME_BATTLE_RANK_S = 3;</code>
     */
    FEVER_TIME_BATTLE_RANK_S("FEVER_TIME_BATTLE_RANK_S", 3),

    /**
     * <code>FEVER_TIME_BATTLE_RANK_SS = 4;</code>
     */
    FEVER_TIME_BATTLE_RANK_SS("FEVER_TIME_BATTLE_RANK_SS", 4);

    /**
     * <code>FEVER_TIME_BATTLE_RANK_C = 0;</code>
     */
    public static final int FEVER_TIME_BATTLE_RANK_C_VALUE = 0;

    /**
     * <code>FEVER_TIME_BATTLE_RANK_B = 1;</code>
     */
    public static final int FEVER_TIME_BATTLE_RANK_B_VALUE = 1;

    /**
     * <code>FEVER_TIME_BATTLE_RANK_A = 2;</code>
     */
    public static final int FEVER_TIME_BATTLE_RANK_A_VALUE = 2;

    /**
     * <code>FEVER_TIME_BATTLE_RANK_S = 3;</code>
     */
    public static final int FEVER_TIME_BATTLE_RANK_S_VALUE = 3;

    /**
     * <code>FEVER_TIME_BATTLE_RANK_SS = 4;</code>
     */
    public static final int FEVER_TIME_BATTLE_RANK_SS_VALUE = 4;

    private final String name;

    private final int number;

    private FeverTimeActivityType(String name, int number) {
      this.name = name;
      this.number = number;
    }

    /**
     * @return the string representation of enum entry
     */
    @Override
    public String getName() {
      return name;
    }

    /**
     * @return the numeric wire value of this enum entry
     */
    @Override
    public int getNumber() {
      return number;
    }

    /**
     * @return a converter that maps between this enum's numeric and text representations
     */
    public static ProtoEnum.EnumConverter<FeverTimeActivityType> converter() {
      return FeverTimeActivityTypeConverter.INSTANCE;
    }

    /**
     * @param value The numeric wire value of the corresponding enum entry.
     * @return The enum associated with the given numeric wire value, or null if unknown.
     */
    public static FeverTimeActivityType forNumber(int value) {
      return FeverTimeActivityTypeConverter.INSTANCE.forNumber(value);
    }

    /**
     * @param value The numeric wire value of the corresponding enum entry.
     * @param other Fallback value in case the value is not known.
     * @return The enum associated with the given numeric wire value, or the fallback value if unknown.
     */
    public static FeverTimeActivityType forNumberOr(int number, FeverTimeActivityType other) {
      FeverTimeActivityType value = forNumber(number);
      return value == null ? other : value;
    }

    enum FeverTimeActivityTypeConverter implements ProtoEnum.EnumConverter<FeverTimeActivityType> {
      INSTANCE;

      private static final FeverTimeActivityType[] lookup = new FeverTimeActivityType[5];

      static {
        lookup[0] = FEVER_TIME_BATTLE_RANK_C;
        lookup[1] = FEVER_TIME_BATTLE_RANK_B;
        lookup[2] = FEVER_TIME_BATTLE_RANK_A;
        lookup[3] = FEVER_TIME_BATTLE_RANK_S;
        lookup[4] = FEVER_TIME_BATTLE_RANK_SS;
      }

      @Override
      public final FeverTimeActivityType forNumber(final int value) {
        if (value >= 0 && value < lookup.length) {
          return lookup[value];
        }
        return null;
      }

      @Override
      public final FeverTimeActivityType forName(final CharSequence value) {
        if (value.length() == 24) {
          if (ProtoUtil.isEqual("FEVER_TIME_BATTLE_RANK_C", value)) {
            return FEVER_TIME_BATTLE_RANK_C;
          }
          if (ProtoUtil.isEqual("FEVER_TIME_BATTLE_RANK_B", value)) {
            return FEVER_TIME_BATTLE_RANK_B;
          }
          if (ProtoUtil.isEqual("FEVER_TIME_BATTLE_RANK_A", value)) {
            return FEVER_TIME_BATTLE_RANK_A;
          }
          if (ProtoUtil.isEqual("FEVER_TIME_BATTLE_RANK_S", value)) {
            return FEVER_TIME_BATTLE_RANK_S;
          }
        }
        if (value.length() == 25) {
          if (ProtoUtil.isEqual("FEVER_TIME_BATTLE_RANK_SS", value)) {
            return FEVER_TIME_BATTLE_RANK_SS;
          }
        }
        return null;
      }
    }
  }
}
